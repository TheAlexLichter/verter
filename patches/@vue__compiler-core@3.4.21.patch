diff --git a/dist/compiler-core.cjs.js b/dist/compiler-core.cjs.js
index 108388bdf1a6aa84fd1b01385ed562b393ae1ba8..982bca180b8076a9df33f713633a1a0a7aa61df5 100644
--- a/dist/compiler-core.cjs.js
+++ b/dist/compiler-core.cjs.js
@@ -745,7 +745,7 @@ class Tokenizer {
     }
   }
   stateInClosingTagName(c) {
-    if (c === 62 || isWhitespace(c)) {
+    if (c === 62) {
       this.cbs.onclosetag(this.sectionStart, this.index);
       this.sectionStart = -1;
       this.state = 10;
@@ -2175,7 +2175,7 @@ const tokenizer = new Tokenizer(stack, {
     endOpenTag(end);
   },
   onclosetag(start, end) {
-    const name = getSlice(start, end);
+    const name = getSlice(start, end).trimEnd();
     if (!currentOptions.isVoidTag(name)) {
       let found = false;
       for (let i = 0; i < stack.length; i++) {
diff --git a/dist/compiler-core.esm-bundler.js b/dist/compiler-core.esm-bundler.js
index 9b03de7c6f12dfdc2280a8ecf3112687488f2c46..8ece95f74d3ad8ee5cdb168933a528408d3bb58b 100644
--- a/dist/compiler-core.esm-bundler.js
+++ b/dist/compiler-core.esm-bundler.js
@@ -728,7 +728,7 @@ class Tokenizer {
     }
   }
   stateInClosingTagName(c) {
-    if (c === 62 || isWhitespace(c)) {
+    if (c === 62) {
       this.cbs.onclosetag(this.sectionStart, this.index);
       this.sectionStart = -1;
       this.state = 10;
@@ -1936,7 +1936,7 @@ const tokenizer = new Tokenizer(stack, {
     endOpenTag(end);
   },
   onclosetag(start, end) {
-    const name = getSlice(start, end);
+    const name = getSlice(start, end).trimEnd();
     if (!currentOptions.isVoidTag(name)) {
       let found = false;
       for (let i = 0; i < stack.length; i++) {
